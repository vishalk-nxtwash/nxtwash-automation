name: Admin Suite (staging)

on:
  push:
    branches: [staging]
  workflow_dispatch:
  schedule:
    - cron: "0 3 * * 0"
  repository_dispatch:
    types: [staging-deploy-complete]

concurrency:
  group: admin-suite-staging
  cancel-in-progress: false

jobs:
  admin:
    strategy:
      fail-fast: false
      max-parallel: 6
      matrix:
        shard:
          - name: discounts
            path: tests/admin_portal/discounts

          # parallel=2: worker-1 handles all managed_membership tests (xdist_group
          # pins them to one worker); worker-2 runs the 49 browser-only tests
          # (search/filter, UI, validation) concurrently. timeout=180 because
          # 17 managed teardowns × ~4-6 min each = up to 102 min on slow staging.
          - name: memberships
            path: tests/admin_portal/memberships
            parallel: "2"
            timeout: 180

          - name: gift-cards
            path: tests/admin_portal/gift_cards tests/admin_portal/coupon_packages

          # --- queued when ready (one at a time, runners are busy with the 3 above) ---
          # - name: wash-packages
          #   path: tests/admin_portal/wash_packages
          # - name: wash-books
          #   path: tests/admin_portal/wash_books
          # - name: wash-extras
          #   path: tests/admin_portal/wash_extras
          # - name: sites
          #   path: tests/admin_portal/sites
          # - name: service-categories
          #   path: tests/admin_portal/service_categories
          # - name: overview
          #   path: tests/admin_portal/overview
          # - name: login
          #   path: tests/admin_portal/login

    uses: ./.github/workflows/run-suite.yml
    secrets:
      dotenv: ${{ secrets.STAGING }}
    with:
      label: admin-${{ matrix.shard.name }}
      environment: staging
      test_path: ${{ matrix.shard.path }}
      parallel: ${{ matrix.shard.parallel || '1' }}
      timeout_minutes: ${{ matrix.shard.timeout || 120 }}
